---
phase: 10
plan: 3
wave: 2
depends_on: [1, 2]
files_modified:
  - server/src/routes/evaluationRoutes.ts
  - server/src/controllers/evaluationController.ts
autonomous: true

must_haves:
  truths:
    - "Backend provides an endpoint to submit interactive question answers for reading and listening."
    - "Answers are verified against the correct answers and a score is returned."
  artifacts:
    - "New POST endpoint in evaluation routes"
---

# Plan 10.3: Backend Objective Test Evaluation

<objective>
Create a new endpoint in the backend for grading interactive Reading and Listening questions (MCQ, gap-fill) programmatically.

Purpose: Provide immediate feedback to students for their objective question submissions.
Output: Evaluation controller and route added.
</objective>

<context>
Load for context:
- new_project/server/src/routes/evaluationRoutes.ts
- new_project/server/src/controllers/evaluationController.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Objective Evaluation Endpoint</name>
  <files>new_project/server/src/controllers/evaluationController.ts, new_project/server/src/routes/evaluationRoutes.ts</files>
  <action>
    Add a POST endpoint `/api/evaluate/objective`.
    The endpoint should receive `{ materialId, answers: Record<string, string> }`.
    Fetch the material from the DB, compare the provided answers with the true answers in the material content JSON, and return a score breakdown.
  </action>
  <verify>curl or similar request to the new endpoint returns a valid grading response.</verify>
  <done>Endpoint correctly grades objective questions against the actual data.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Backend provides an endpoint to submit interactive question answers for reading and listening.
- [ ] Answers are verified against the correct answers and a score is returned.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
