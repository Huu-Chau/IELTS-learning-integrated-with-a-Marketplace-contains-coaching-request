export enum MarketplaceRequestStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    REJECTED = 'rejected',
}

export enum MarketplaceRequestType {
    TARGETED = 'targeted',
    BROADCAST = 'broadcast',
    BOOKING = 'booking',
}
